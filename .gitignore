venv/
__pycache__/
*.pyc
.streamlit/

# Raw Kaggle dump (516MB+, exceeds GitHub's file size limit, and datasets
# don't belong in git history anyway - see README for how to regenerate)
data/postings.csv
data/indeed-job-listings.csv
data/companies/
data/jobs/
data/mappings/
data/*.zip

# Derived/intermediate datasets - regeneratable via prepare_data.py and
# build_features.py, and postings_features.csv alone is ~100MB
data/postings_clean.csv
data/postings_features.csv

# Kaggle credentials
.kaggle/
kaggle.json
